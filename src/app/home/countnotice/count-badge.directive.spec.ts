import { CountBadgeDirective } from './count-badge.directive';

describe('CountBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new CountBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
