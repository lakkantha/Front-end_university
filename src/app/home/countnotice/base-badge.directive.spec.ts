import { BaseBadgeDirective } from './base-badge.directive';

describe('BaseBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
